'use strict'

for (let i = 0; i < 21; i++) { //Create a for loop, that iterates from 0 to 20. & Create a conditional statement that checks if the value of the counter variable is odd or even.
    (i % 2 === 0) ? console.log(`The number ${i} is even!`): console.log(`The number ${i} is odd!`);
    /*If it's odd, log to the console The number [PUT_NUMBER_HERE] is odd!.
       If it's even, log to the console The number [PUT_NUMBER_HERE] is even!.*/
}